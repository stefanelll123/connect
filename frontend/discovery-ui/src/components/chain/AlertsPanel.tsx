/**
 * AlertsPanel — reserved for future use.
 * The /chain/alerts endpoint was removed from the backend; this component is a no-op stub.
 */
export interface AlertsPanelProps {
  // no props
}

export const AlertsPanel: React.FC<AlertsPanelProps> = () => null;

import React from "react";

